#include<iostream>
int main(){
    std::cout<<"hi kunal"<<std::endl;
    std::cout<<"hi bro!"<<"\n";
    return 0;
}
//namespace is like a container which is used to group names like(variables,function,classes) to avoid naming conflict in the program.
//two ways for creating line breaks: 1)"\n" and 2)endl.
//"\n" is simple and more fast and efficient.
//endl is used for flusing output buffer and it is costly.
//Adding using namespace std; eliminates the need to prefix std:: repeatedly