#include<iostream>
using namespace std;
int main(){
    int length;
    int breadth;
    int area;
    cout<<"enter the length: ";
    cin>>length;
    cout<<"enter the breadth: ";
    cin>>breadth;
    area = length * breadth;
    cout<<"the area of rectangle is "<<area;
    return 0;
}